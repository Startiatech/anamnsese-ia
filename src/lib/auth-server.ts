// Re-export from new location — use @/server/services/session directly in new code
export { getServerUser } from '@/server/services/session'
export type { JWTPayload } from '@/server/services/session'
